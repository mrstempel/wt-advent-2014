@{
    Layout = null;
	var cssClass = Convert.ToInt32(ViewBag.Points) > 0 ? "bonus" : string.Empty;
}

<script>
	$(document).ready(function()
	{
		initVideo();
	});

	function initVideo()
	{
		$('#video-placeholder').hide();
		var videoSize = new Object();
		videoSize.width = $('#video-placeholder').innerWidth();
		videoSize.height = Math.round(videoSize.width / video_aspect);
		init_yt_playerAutoPlay(videoSize, '624hbQ8q3qs', 'day11-video');
	}

	$(window).resize(function()
	{
		resizePlayer('day11-video');
	});
</script>

<div class="modal-header">
	<span class="btn-close" onclick="jQuery('#day11-video').tubeplayer('destroy');hideModal('more-info', @ViewBag.Points)"></span>
</div>
<div class="modal-body">
	<article class="@cssClass">
		<h1>@Translate("antwort11-headline")</h1>
		<h2>@Translate("antwort11-text")</h2>
		<div class="content" id="video-content">
			<img src="/Images/placeholder.gif" style="width:855px;height:481px;" id="video-placeholder"/>
			<div class="video-container" id="day11-video"></div>
		</div>
		<div class="content-description">@Translate("antwort11-artikel-content-beschreibung")</div>
	</article>
	@Html.Partial("_MoreInfoFooter",null)
</div>
